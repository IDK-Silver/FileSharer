# =============================================================================
# FileSharer Docker Compose Configuration
# =============================================================================

# MySQL Database Settings for Docker Compose
MYSQL_HOST=db
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=your_root_password_here
MYSQL_DATABASE=file_sharer
MYSQL_USER=admin
MYSQL_PASSWORD=your_password_here

# =============================================================================
# Shared Storage Configuration
# =============================================================================

# Storage Provider Selection: "s3" | "gcs"
STORAGE_PROVIDER=s3

# MinIO Configuration (default for local development)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
S3_BUCKET_NAME=file-sharer-system-storage

# =============================================================================
# Note: Frontend and Backend specific settings are in their respective .env files
# - Frontend settings: ./frontend/.env
# - Backend settings: ./backend/.env
# =============================================================================